{
  "name": "bimsc-knack-api",
  "description": "creates payload and makes calls to the knack api",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "node esbuild.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "knack-api-helper": "^2.2.6"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
